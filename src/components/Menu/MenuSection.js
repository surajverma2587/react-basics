import React from 'react';
/* import PropTypes, child components and styles here */

const renderMenuItems = (/* menuItems */) => {
  // map through the menuItems and return list of MenuItem components
};

const MenuSection = (/* props object */) => {
  /* bind the data from the props object to the html template */
  /* validate the props and render the component IF valid ELSE null */
  /* remember to call renderMenuItems() to render the list of MenuItem components */
  /* replace the bottom return statement with the populated html template */
};

/* display name for the component */
MenuSection.displayName = 'MenuSection';

MenuSection.propTypes = {
  /* add Proptypes here */
};

MenuSection.defaultProps = {
  /* add default values to required props */
};

export default MenuSection;
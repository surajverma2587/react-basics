import React from 'react';
/* import the child components and styles here */

const renderMenuSections = (/* menuSections */) => {
  // return list of MenuSection components
};

const Menu = (/* props object */) => {
  /* bind the props object data to your html template data requirement */
  /* remember to map through each menu section and return the MenuSection component */
  /* return the populated html template */
};

export default Menu;
import React from 'react';
/* import the child components and styles here */

const renderCartItems = (/* cartItems */) => {
  // return list of CartItem components
}

const Cart = (/* props object */) => {
  /* bind the props object data to your html template data requirement */
  /* remember to map through each cart item and return the CartItem component */
  /* return the populated html template */
};

export default Cart;
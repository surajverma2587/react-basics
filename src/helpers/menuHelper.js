const getSelectedItem = (/* params */) => {
  /* this function will iterate over every menu item within a menu section */
  /* and filter the menu item for a given menu item id */
}

const deleteSelectedItem = (/* params */) => {
  /* this function will iterate over every menu item for a given list */
  /* and delete the menu item for a given menu item id from that list */
}

export { getSelectedItem, deleteSelectedItem }